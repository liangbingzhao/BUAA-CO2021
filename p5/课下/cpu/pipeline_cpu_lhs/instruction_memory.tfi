


// Instantiate the module
instruction_memory instance_name (
    .clk(clk), 
    .reset(reset), 
    .M_PC(M_PC), 
    .M_instr(M_instr), 
    .M_WD(M_WD), 
    .M_ALUResult(M_ALUResult), 
    .M_Forward2(M_Forward2), 
    .M_WD_W(M_WD_W)
    );


