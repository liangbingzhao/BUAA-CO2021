


// Instantiate the module
datapath instance_name (
    .clk(clk), 
    .reset(reset), 
    .op(op), 
    .func(func), 
    .rt(rt), 
    .sign(sign), 
    .Branch(Branch), 
    .MemWrite(MemWrite), 
    .RegWrite(RegWrite), 
    .MemtoReg(MemtoReg), 
    .ALUsrc(ALUsrc), 
    .RegDst(RegDst), 
    .ALUControl(ALUControl), 
    .PCj(PCj), 
    .jalsave(jalsave), 
    .jr(jr), 
    .BNEZALC_fake(BNEZALC_fake)
    );


